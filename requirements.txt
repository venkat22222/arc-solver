# Core Runtime & Testing
pytest>=8.0.0
pyyaml>=6.0.1
numpy>=1.26.0
scipy>=1.12.0
python-dotenv>=1.0.0
requests>=2.31.0

# Kaggle / In-Process GPU LLM Backend (Qwen3-8B 4-bit NF4)
# Note: Install CUDA-compatible PyTorch first (e.g. cu124)
transformers>=4.51.0,<5.0.0
accelerate>=0.33.0,<1.3.0
bitsandbytes>=0.43.0,<0.46.0
huggingface-hub>=0.30.0,<1.0.0

# Optional Cloud API Backend (Gemini Development / Benchmarking)
google-generativeai>=0.8.0
